# Environment variables for chatbot_frontend (Create React App)
# Copy this file to ".env" and adjust values as needed.

# PUBLIC_INTERFACE
# BACKEND API base URL used by the frontend to call the Django backend.
# If not set, the app falls back to '/api' which works with CRA proxy in local dev.
# For local/dev pointing to a backend on port 3001 with base path /api:
REACT_APP_BACKEND_API_URL=http://localhost:3001/api
